import { ReaderArguments, ReaderReturn } from "../types";

export default function usePdfReader(args: ReaderArguments): ReaderReturn {
  // console.error("Attempting to usePdfReader");
  return null;
}
